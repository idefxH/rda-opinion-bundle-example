# Tiltfile for {{ .Name }} — generated by rda from web-nodejs template.
#
# Uses the SUSE Tilt extension (idefxH/tilt-extension-suse-rda) which wraps
# Cloud Native Buildpacks (via pack), Helm install, and service port-forwards
# into a single declarative call.
#
# Pinned to a tag in production; main here for fast iteration during v0.

load_dynamic('https://raw.githubusercontent.com/idefxH/tilt-extension-suse-rda/main/Tiltfile')

suse_app(
    name='{{ .Name }}',
    language='{{ .Language }}',
    port=8080,
    services={'database': 'postgresql'},
)
