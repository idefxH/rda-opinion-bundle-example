# Tiltfile for {{ .Name }} — generated by rda from web-nodejs template.
#
# Loads the SUSE Tilt extension (idefxH/tilt-extension-suse-rda) which wraps
# Cloud Native Buildpacks (via pack), Helm install, and service port-forwards
# into a single declarative call.
#
# load_dynamic returns the extension's symbols as a dict; we extract the
# functions we want into the current namespace.

_ext = load_dynamic('https://raw.githubusercontent.com/idefxH/tilt-extension-suse-rda/main/Tiltfile')
suse_app = _ext['suse_app']

suse_app(
    name='{{ .Name }}',
    language='{{ .Language }}',
    port=8080,
    services={'database': 'postgresql'},
)
