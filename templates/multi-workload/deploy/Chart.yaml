apiVersion: v2
name: {{ .Name }}
description: {{ .Name }} — multi-workload project (api + worker)
type: application
version: 0.1.0
appVersion: "0.1.0"
keywords: [rda, multi-workload]

dependencies:
  - name: suse-library
    version: 0.16.0
