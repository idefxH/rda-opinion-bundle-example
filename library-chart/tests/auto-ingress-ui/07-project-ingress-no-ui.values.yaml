services: []
workloads_resolved:
  - name: web
    port: 8080
    image:
      repository: nginx
      tag: latest
    ingress:
      hosts:
        - demo.localhost
grafana: {enabled: false}
prometheus: {enabled: false}
