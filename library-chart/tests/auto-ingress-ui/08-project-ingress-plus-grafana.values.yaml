services: []
ingress:
  enabled: true
workloads_resolved:
  - name: web
    port: 8080
    image:
      repository: nginx
      tag: latest
    ingress:
      hosts:
        - app.localhost
grafana:
  enabled: true
  adminPassword: x
  ui: {expose: true}
