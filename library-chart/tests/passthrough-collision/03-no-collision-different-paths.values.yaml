services:
  - binding: database
    type: postgresql
    auth:
      user:
        name: app
        password: dev-pw
        database: demo
    persistence:
      enabled: true
    passthrough:
      primary:
        nodeSelector:
          disk: ssd

postgresql:
  enabled: true
