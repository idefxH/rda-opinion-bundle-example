services:
  - binding: database
    type: postgresql
    auth:
      user:
        name: app
        password: dev-pw
        database: demo
    metrics:
      enabled: true
    passthrough:
      metrics:
        serviceMonitor:
          enabled: true
