services:
  - binding: vault
    type: postgresql
    provisioning: connect          # no defaults.shared_services configured → must fail loud
    auth:
      user:
        name: app
        password: x
        database: y
